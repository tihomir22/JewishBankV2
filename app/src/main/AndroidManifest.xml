<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simarro.practica.jewishbank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize">
        <activity android:name=".BienvenidaActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".PostLogin" />
        <activity android:name=".cambiarContrasenya" />
        <activity android:name=".Transferencias" />
        <activity android:name=".RegistroActividad" />
        <activity
            android:name=".PosicionGlobal"
            android:label="@string/title_activity_posicion_global"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TransferenciasActivity"></activity>
    </application>

</manifest>